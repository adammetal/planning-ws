// scope:
// global
// module
// fucntion
// block

import { noun, werb, dict } from "./data";

function seed(input) {
  return input.length;
}

function brandNameGen(input)
{
  const seed = input.length;

  if (seed >= noun.length) {
    const n = noun.length - 1;
  }

  return "brand name";
} 

export default brandNameGen;
